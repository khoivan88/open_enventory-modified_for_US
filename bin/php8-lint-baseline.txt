# Files that fail 'php -l' under PHP 8 purely because of PHP 5/7-era
# syntax this codebase still uses (curly-brace string offsets, 'readonly'
# as a bare constant, and similar). They parse correctly under the
# supported PHP 7.4. Format: <path>\t<message>\t<line>
# Regenerate with: bin/lint-php.sh --update-baseline
lib_draw_analytics.php	Class specImage cannot extend final class GdImage	325
